---
title: <機能名>
slug: <NN>-<feature-slug>
scope: <この spec が扱う範囲を 1 行で>
audience: サークル運営者・新メンバー
relatedPrd:
  - .claude/PRPs/<NN>-<slug>/prds/<NN>-<slug>.prd.md
targetPhase: <"Phase X.Y 完了時点" 等>
lastUpdated: YYYY-MM-DD
status: stable
---

# <機能名> 業務仕様書

## 1. 概要

<1〜2 段落で「この機能は何ができるか」「主にどんな人が使うか」「どの画面で操作するか」>

## 2. 想定利用シーン

### 2.1 ペルソナ

- **<ペルソナ名 1>**: <役割 / 主目的を 1 行>
- **<ペルソナ名 2>**: <役割 / 主目的を 1 行>

### 2.2 シナリオ

#### 2.2.1 平常系: <シナリオ名>

<いつ・誰が・どこから始める → どんな操作 → どうなる。5〜10 行>

#### 2.2.2 失敗系: <シナリオ名>

<通信エラー / 招待リンク切れ / 同時編集 / 重複登録 など>

#### 2.2.3 運用イベント系: <シナリオ名>

<シーズン切替 / 役割昇降格 / 設定変更 など。該当なしなら「該当なし。理由: …」と明記>

## 3. 機能要件

### 3.1 <機能名 1>

#### 3.1.1 何ができる機能か

<1〜2 行要約 → 詳細を箇条書きで（業務目線で「画面・操作・結果」を語る）>

#### 3.1.2 誰が・いつ使うか

<ペルソナと典型的なタイミング>

#### 3.1.3 なぜこの機能が必要か（Why）

<サークル運営の何の課題を解くか / この機能がない場合の困りごと。PRD の動機を業務語彙で引用>

#### 3.1.4 設定項目とその意味

| 項目 | 入力形式 | 推奨 / 既定値 | 業務上の意味 |
| --- | --- | --- | --- |
| <項目名> | <例: 文字列、最大 N 文字> | <既定値> | <なぜこの値が必要か> |

設定項目がない場合は「該当なし」と明記。

#### 3.1.5 注意点・制約

- <1 度設定すると変更不可 / 上限 N / 期限切れの扱い 等>
- <最後のオーナーは脱退できない 等の運用上の落とし穴>

### 3.2 <機能名 2>

<同上。Phase 2 走査の業務機能リストと突き合わせ、漏らさず列挙する>

## 4. 権限・認可

各 spec に inline で権限マトリクスを必ず記載。overview や他 spec への委譲のみで済ませない。

### 4.1 ロール別操作マトリクス

| 操作 | オーナー | 運営担当者 | 一般メンバー | ゲスト（匿名） |
| --- | --- | --- | --- | --- |
| <操作名 1> | ○ | ○ | × | × |
| <操作名 2> | ○ | ○ | ○ | × |
| <操作名 3> | ○ | × | × | × |

凡例: ○ 可 / × 不可 / △ 条件付き（条件は表下に補足）

### 4.2 ロール定義（spec 内の文脈で）

- **オーナー**: <この spec で「オーナー」とはどういう人か>
- **運営担当者**: <同上>
- **一般メンバー**: <同上>
- **ゲスト（匿名）**: <該当する場合のみ。なければ削除可>

### 4.3 補足

<役割の昇降格は本 spec 範囲か / ロールの前提条件 / 例外ケース>

## 5. 非機能要件（業務観点）

7 観点を**すべて**取り上げる。該当なしの場合は「該当なし。理由: …」と明記。

### 5.1 パフォーマンス（体感）

<ボタンを押してから何秒で結果が出るか / 同時に何人で操作しても大丈夫か>

### 5.2 セキュリティ（運用方針）

<誰が誰のデータを見られるか / 削除できるか / 招待リンクの安全性>

### 5.3 使いやすさ / アクセシビリティ

<キーボード・スマホ対応 / 文字サイズ / 色のコントラスト / スクリーンリーダー>

### 5.4 信頼性 / エラー時の挙動

<通信エラー時の表示 / 二重実行の防止 / オフライン時>

### 5.5 保守性 / 運用負担

<設定変更にかかる手間 / 設定値が連動する箇所>

### 5.6 互換性

<対応ブラウザ / 端末 / 既存データの扱い>

### 5.7 運用 / 観測性

<記録される操作 / 障害時の確認ポイント>

## 6. 既知の制約・未対応事項

- <PRD で未着手の項目を業務語彙で>
- <運用上踏みやすい落とし穴>
- <将来の改善方針（短く）>

## 7. 関連 spec / ドキュメント

- 親 spec: [00-overview.spec.md](00-overview.spec.md)
- 関連 spec: [<NN>-<slug>.spec.md](<NN>-<slug>.spec.md)
- 関連 PRD（参考、設計の意思決定背景）: [<NN>-<slug>.prd.md](../../.claude/PRPs/<NN>-<slug>/prds/<NN>-<slug>.prd.md)
- 用語の翻訳ガイド: [.claude/skills/spec-writer/references/glossary.md](../../.claude/skills/spec-writer/references/glossary.md)

## 8. 網羅性チェック

### 対応する画面 / 設定項目 / 想定利用シーン

- 画面: `<URL パス>`
- 主な操作: <箇条書き>
- 設定項目: <箇条書き>
- 関連シーン: <箇条書き>
- 関連 PRD: <NN>-<slug>.prd.md の Phase X-Y
